install venv, fastapi, unvicorn
docs, redoc
Enum, pydantic, validator, annotated type -> filter (in + out(respond_model))
path parameters, query parameters -> request
http exception, state
decorator -> update a func; wraps -> keep all the data after decorator
python package -> truy cap de hon
posgreSQL -> database
SQL -> db lang + psycopg2 -> use SQL in code
ORM: sqlalchemy vs sqlmodel -> connect to db without SQL
connect to db -> create table -> connect to api
users + posts + login + register + vote/like
web security: hash password, JWT Token, expire time
routers + prefix & tags -> more clean
relationship: foreign key, user_info
environment variables
database migration - alembic
